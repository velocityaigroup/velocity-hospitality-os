# Core
boto3>=1.34            # AWS SDK (Bedrock, Lambda, DynamoDB, Step Functions)
pydantic>=2.6          # typed models for agent inputs/outputs
python-dotenv>=1.0     # local env loading

# Agentic / LLM (open-source friendly)
# Swap or pin to the framework chosen during the sprint.
# langgraph>=0.0.40    # multi-agent orchestration (optional)
# litellm>=1.40        # provider-agnostic LLM calls (optional)

# Dev
pytest>=8.0
ruff>=0.4
